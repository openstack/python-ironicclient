---
deprecations:
  - |
    The ``ironic`` command line interface (``ironic`` commands) is deprecated
    and will be removed in the OpenStack S* release.  Please use the
    ``openstack baremetal`` command line interface instead.
