---
fixes:
  - No longer emits the incorrect warning "Please specify what to set"
    (or "unset") when only the ``--target-raid-config`` is specified in the
    ``openstack baremetal node set`` (or ``unset``) command.
